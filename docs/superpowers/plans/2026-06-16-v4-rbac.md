# V4 RBAC Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add OWNER/MEMBER/VIEWER roles to projects, allow owners to invite members by email, and restrict actions based on role.

**Architecture:** A `ProjectMember` join table stores the role per (project, user) pair. A `requireProjectRole` helper replaces the existing `verifyProjectOwner` pattern across all routes. The frontend adds a members tab to the project detail page with role-gated UI.

**Tech Stack:** Hono + Prisma + PostgreSQL (backend, port 3000), Next.js + TanStack Query + Axios (frontend, port 3001). Package manager: **pnpm** (never npm).

---

## Files

**Backend — modified:**
- `prisma/schema.prisma` — add back-relations `members` on Project and `projectMembers` on User
- `src/routes/projects.ts` — update GET/ (membership filter + userRole), POST/ (create OWNER member), GET/:id and DELETE/:id (role checks)
- `src/routes/tasks.ts` — replace `verifyProjectOwner` with `requireProjectRole`
- `src/index.ts` — mount members route

**Backend — created:**
- `src/lib/projectAuth.ts` — `requireProjectRole` helper with ROLE_RANK
- `src/routes/members.ts` — GET/POST/PATCH/DELETE for project members
- `src/validators/members.ts` — Zod schemas for member routes
- `src/scripts/migrate-v4.ts` — one-time data migration for existing projects

**Frontend — modified:**
- `frontend/src/types/project.ts` — add `Role`, `ProjectMember` types; add `userRole` to `Project`
- `frontend/src/app/dashboard/page.tsx` — role badge on project cards
- `frontend/src/app/dashboard/[id]/page.tsx` — tabs + members UI + role-gated buttons

---

## Task 1: Fix Prisma schema back-relations

**Files:**
- Modify: `prisma/schema.prisma`

The schema already has the `Role` enum and `ProjectMember` model, but `Project` and `User` are missing the required back-relation fields — `prisma generate` and `prisma db push` will fail without them.

- [ ] **Step 1: Add back-relations to `User` and `Project`**

In `prisma/schema.prisma`, update the two models:

```prisma
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  emailVerified Boolean   @default(false)
  name          String?
  image         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  sessions        Session[]
  accounts        Account[]
  projects        Project[]
  tasks           Task[]
  projectMembers  ProjectMember[]
}

model Project {
  id          String        @id @default(cuid())
  name        String
  description String?
  status      ProjectStatus @default(ACTIVE)
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  ownerId     String
  owner       User          @relation(fields: [ownerId], references: [id])
  tasks       Task[]
  members     ProjectMember[]
}
```

- [ ] **Step 2: Push schema to database**

Run from the repo root:

```bash
pnpm prisma db push
```

Expected output: `Your database is now in sync with your Prisma schema.`

- [ ] **Step 3: Create one-time data migration script**

Create `src/scripts/migrate-v4.ts`:

```ts
import { db } from '../db/client'

async function main() {
  const projects = await db.project.findMany({ select: { id: true, ownerId: true } })
  for (const project of projects) {
    await db.projectMember.upsert({
      where: { projectId_userId: { projectId: project.id, userId: project.ownerId } },
      update: {},
      create: { projectId: project.id, userId: project.ownerId, role: 'OWNER' },
    })
  }
  console.log(`Migrated ${projects.length} projects`)
}

main().catch(console.error).finally(() => db.$disconnect())
```

- [ ] **Step 4: Run the migration script**

```bash
pnpm tsx src/scripts/migrate-v4.ts
```

Expected output: `Migrated N projects`

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma src/scripts/migrate-v4.ts
git commit -m "chore: add ProjectMember back-relations and v4 data migration script"
```

---

## Task 2: Create `requireProjectRole` helper

**Files:**
- Create: `src/lib/projectAuth.ts`

This helper is called inside route handlers (not as middleware). It returns `{ ok: true, member }` or `{ ok: false, response }`. Routes check `auth.ok` and return `auth.response` early on failure.

- [ ] **Step 1: Create `src/lib/projectAuth.ts`**

```ts
import { Context } from 'hono'
import { db } from '../db/client'

type Role = 'OWNER' | 'MEMBER' | 'VIEWER'

const ROLE_RANK: Record<Role, number> = { VIEWER: 0, MEMBER: 1, OWNER: 2 }

export async function requireProjectRole(
  c: Context,
  projectId: string,
  minRole: Role
) {
  const user = c.get('user') as { id: string }
  const member = await db.projectMember.findUnique({
    where: { projectId_userId: { projectId, userId: user.id } },
  })
  if (!member || ROLE_RANK[member.role as Role] < ROLE_RANK[minRole]) {
    return {
      ok: false as const,
      response: c.json({ error: { code: 'FORBIDDEN', message: 'Access denied' } }, 403),
    }
  }
  return { ok: true as const, member }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/projectAuth.ts
git commit -m "feat: add requireProjectRole helper"
```

---

## Task 3: Update `POST /projects` to create OWNER membership

**Files:**
- Modify: `src/routes/projects.ts`

When a project is created, atomically insert a `ProjectMember` row with `role: OWNER` using Prisma nested create.

- [ ] **Step 1: Replace the POST handler**

In `src/routes/projects.ts`, replace `projects.post("/", ...)` with:

```ts
projects.post("/", async (c) => {
  const user = c.get('user')
  const body = await c.req.json()
  const result = CreateProjectSchema.safeParse(body)
  if (!result.success) {
    return c.json({ error: result.error.flatten() }, 422)
  }
  const project = await db.project.create({
    data: {
      name: result.data.name,
      description: result.data.description,
      ownerId: user.id,
      members: {
        create: { userId: user.id, role: 'OWNER' },
      },
    },
  })
  return c.json({ data: project }, 201)
})
```

- [ ] **Step 2: Commit**

```bash
git add src/routes/projects.ts
git commit -m "feat: auto-create OWNER membership on project creation"
```

---

## Task 4: Update `GET /projects` to filter by membership

**Files:**
- Modify: `src/routes/projects.ts`

Replace the `ownerId`-based filter with a `ProjectMember`-based query. Include `userRole` in each returned project so the frontend can display role badges and gate UI.

- [ ] **Step 1: Replace the GET / handler**

In `src/routes/projects.ts`, replace `projects.get("/", ...)` with:

```ts
projects.get("/", async (c) => {
  const user = c.get('user')
  const memberships = await db.projectMember.findMany({
    where: { userId: user.id },
    include: { project: true },
  })
  const data = memberships
    .filter(m => m.project.status !== 'DELETED')
    .map(m => ({ ...m.project, userRole: m.role }))
  return c.json({ data })
})
```

- [ ] **Step 2: Commit**

```bash
git add src/routes/projects.ts
git commit -m "feat: filter projects by membership and include userRole"
```

---

## Task 5: Add role checks to `GET /:id` and `DELETE /:id`

**Files:**
- Modify: `src/routes/projects.ts`

Replace bare ownership assumptions with `requireProjectRole`. VIEWER can read; only OWNER can delete.

- [ ] **Step 1: Add import**

At the top of `src/routes/projects.ts`, add:

```ts
import { requireProjectRole } from '../lib/projectAuth'
```

- [ ] **Step 2: Replace `GET /:id`**

```ts
projects.get("/:id", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response
  const project = await db.project.findUnique({ where: { id } })
  if (!project) return c.json({ error: { code: 'NOT_FOUND', message: 'Project not found' } }, 404)
  return c.json({ data: project })
})
```

- [ ] **Step 3: Replace `DELETE /:id`**

```ts
projects.delete("/:id", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  await db.project.update({ where: { id }, data: { status: 'DELETED' } })
  return c.body(null, 204)
})
```

- [ ] **Step 4: Commit**

```bash
git add src/routes/projects.ts
git commit -m "feat: add VIEWER/OWNER role checks to project routes"
```

---

## Task 6: Update task routes to use `requireProjectRole`

**Files:**
- Modify: `src/routes/tasks.ts`

Replace the `verifyProjectOwner` pattern (owner-only) with per-route `requireProjectRole` calls:
- GET / and GET /:taskId → VIEWER
- POST /, PATCH /:taskId, DELETE /:taskId → MEMBER

- [ ] **Step 1: Rewrite `src/routes/tasks.ts`**

```ts
import { Hono } from "hono"
import { db } from "../db/client"
import { CreateTaskSchema, UpdateTaskSchema } from '../validators/tasks'
import { requireProjectRole } from '../lib/projectAuth'

type Variables = { user: { id: string; email: string; name?: string | null } }

const tasks = new Hono<{ Variables: Variables }>()

tasks.get("/", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response
  const taskList = await db.task.findMany({ where: { projectId: id } })
  return c.json({ data: taskList })
})

tasks.post("/", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const body = await c.req.json()
  const result = CreateTaskSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const task = await db.task.create({
    data: {
      title: result.data.title,
      description: result.data.description,
      priority: result.data.priority,
      dueDate: result.data.dueDate,
      projectId: id,
    },
  })
  return c.json({ data: task }, 201)
})

tasks.get("/:taskId", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response
  const taskId = c.req.param("taskId")
  const task = await db.task.findUnique({ where: { id: taskId } })
  if (!task) return c.json({ error: { code: "NOT_FOUND", message: "Task not found" } }, 404)
  return c.json({ data: task })
})

tasks.patch("/:taskId", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const taskId = c.req.param("taskId")
  const body = await c.req.json()
  const result = UpdateTaskSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const task = await db.task.update({ where: { id: taskId }, data: result.data })
  return c.json({ data: task })
})

tasks.delete("/:taskId", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const taskId = c.req.param("taskId")
  await db.task.update({ where: { id: taskId }, data: { status: "ARCHIVED" } })
  return c.body(null, 204)
})

export default tasks
```

- [ ] **Step 2: Commit**

```bash
git add src/routes/tasks.ts
git commit -m "feat: enforce VIEWER/MEMBER roles on task routes"
```

---

## Task 7: Create member management routes

**Files:**
- Create: `src/validators/members.ts`
- Create: `src/routes/members.ts`

- [ ] **Step 1: Create `src/validators/members.ts`**

```ts
import { z } from 'zod'

export const AddMemberSchema = z.object({
  email: z.string().email(),
  role: z.enum(['MEMBER', 'VIEWER']),
})

export const UpdateMemberRoleSchema = z.object({
  role: z.enum(['OWNER', 'MEMBER', 'VIEWER']),
})
```

- [ ] **Step 2: Create `src/routes/members.ts`**

```ts
import { Hono } from 'hono'
import { db } from '../db/client'
import { requireProjectRole } from '../lib/projectAuth'
import { AddMemberSchema, UpdateMemberRoleSchema } from '../validators/members'

type Variables = { user: { id: string; email: string; name?: string | null } }

const members = new Hono<{ Variables: Variables }>()

members.get('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response
  const memberList = await db.projectMember.findMany({
    where: { projectId: id },
    include: { user: { select: { id: true, name: true, email: true } } },
    orderBy: { createdAt: 'asc' },
  })
  return c.json({ data: memberList })
})

members.post('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  const body = await c.req.json()
  const result = AddMemberSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const targetUser = await db.user.findUnique({ where: { email: result.data.email } })
  if (!targetUser) return c.json({ error: { code: 'NOT_FOUND', message: 'User not found' } }, 404)
  const existing = await db.projectMember.findUnique({
    where: { projectId_userId: { projectId: id, userId: targetUser.id } },
  })
  if (existing) return c.json({ error: { code: 'CONFLICT', message: 'Already a member' } }, 409)
  const member = await db.projectMember.create({
    data: { projectId: id, userId: targetUser.id, role: result.data.role },
    include: { user: { select: { id: true, name: true, email: true } } },
  })
  return c.json({ data: member }, 201)
})

members.patch('/:userId', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  const userId = c.req.param('userId')
  const body = await c.req.json()
  const result = UpdateMemberRoleSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const member = await db.projectMember.update({
    where: { projectId_userId: { projectId: id, userId } },
    data: { role: result.data.role },
    include: { user: { select: { id: true, name: true, email: true } } },
  })
  return c.json({ data: member })
})

members.delete('/:userId', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  const userId = c.req.param('userId')
  const currentUser = c.get('user') as { id: string }
  if (userId === currentUser.id) {
    return c.json({ error: { code: 'FORBIDDEN', message: 'Cannot remove yourself' } }, 403)
  }
  await db.projectMember.delete({
    where: { projectId_userId: { projectId: id, userId } },
  })
  return c.body(null, 204)
})

export default members
```

- [ ] **Step 3: Commit**

```bash
git add src/routes/members.ts src/validators/members.ts
git commit -m "feat: add member management routes (GET/POST/PATCH/DELETE)"
```

---

## Task 8: Mount members route in `src/index.ts`

**Files:**
- Modify: `src/index.ts`

- [ ] **Step 1: Add import and route**

Add the import after the tasks import:
```ts
import members from './routes/members'
```

Add the route mount after the tasks route (before `app.route('/projects', projects)`):
```ts
app.route('/projects/:id/members', members)
```

The relevant section of `src/index.ts` should look like:
```ts
import projects from './routes/projects'
import tasks from './routes/tasks'
import members from './routes/members'
// ...
app.use('/projects/*', authMiddleware)

app.route('/projects/:id/tasks', tasks)
app.route('/projects/:id/members', members)
app.route('/projects', projects)
```

- [ ] **Step 2: Restart backend and verify**

Restart the backend (`pnpm dev` from root). Then test:

```bash
# List members (replace PROJECT_ID with a real id from your DB)
curl http://localhost:3000/projects/PROJECT_ID/members \
  -H "Cookie: your-session-cookie-from-browser"
```

Expected: `{ "data": [{ "id": "...", "userId": "...", "role": "OWNER", "user": { ... } }] }`

- [ ] **Step 3: Commit**

```bash
git add src/index.ts
git commit -m "feat: mount members route at /projects/:id/members"
```

---

## Task 9: Update frontend types

**Files:**
- Modify: `frontend/src/types/project.ts`

- [ ] **Step 1: Replace the full file content**

```ts
export type ProjectStatus = "ACTIVE" | "ARCHIVED" | "DELETED"
export type Role = "OWNER" | "MEMBER" | "VIEWER"

export interface Project {
  id: string
  name: string
  description?: string
  ownerId: string
  status: ProjectStatus
  createdAt: string
  updatedAt: string
  userRole?: Role
}

export interface CreateProjectInput {
  name: string
  description?: string
}

export interface ProjectMember {
  id: string
  userId: string
  projectId: string
  role: Role
  createdAt: string
  user: {
    id: string
    name: string | null
    email: string
  }
}

export type TaskStatus = "TODO" | "IN_PROGRESS" | "DONE" | "BLOCKED" | "ARCHIVED"
export type TaskPriority = "LOW" | "MEDIUM" | "HIGH" | "CRITICAL"

export interface Task {
  id: string
  title: string
  description?: string
  status: TaskStatus
  priority: TaskPriority
  dueDate?: string
  projectId: string
  assigneeId?: string
  createdAt: string
  updatedAt: string
}
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/types/project.ts
git commit -m "feat: add Role and ProjectMember types, add userRole to Project"
```

---

## Task 10: Update dashboard to show role badges

**Files:**
- Modify: `frontend/src/app/dashboard/page.tsx`

Add role constants and a role badge next to the status badge on each project card.

- [ ] **Step 1: Add role constants at top of component**

At the top of `DashboardPage` (before the `return`), add:

```tsx
const ROLE_LABELS: Record<string, string> = {
  OWNER: 'Propriétaire',
  MEMBER: 'Membre',
  VIEWER: 'Lecteur',
}

const ROLE_COLORS: Record<string, string> = {
  OWNER: 'bg-purple-500/20 text-purple-400',
  MEMBER: 'bg-blue-500/20 text-blue-400',
  VIEWER: 'bg-gray-500/20 text-gray-400',
}
```

- [ ] **Step 2: Update the project card JSX**

Replace the card's `<div className="flex justify-between items-start mb-2">` block with:

```tsx
<div className="flex justify-between items-start mb-2">
  <h2 className="font-bold text-white">{project.name}</h2>
  <div className="flex items-center gap-2">
    {project.userRole && (
      <span className={`text-xs px-2 py-1 rounded ${ROLE_COLORS[project.userRole]}`}>
        {ROLE_LABELS[project.userRole]}
      </span>
    )}
    <span className={`text-xs px-2 py-1 rounded ${
      project.status === 'ACTIVE'
        ? 'bg-green-500/20 text-green-400'
        : 'bg-gray-500/20 text-gray-400'
    }`}>
      {project.status}
    </span>
  </div>
</div>
```

- [ ] **Step 3: Verify in browser**

Open http://localhost:3001/dashboard. Each project card should show a purple "Propriétaire" badge.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/app/dashboard/page.tsx
git commit -m "feat: display role badge on dashboard project cards"
```

---

## Task 11: Add tabs and members UI to project detail page

**Files:**
- Modify: `frontend/src/app/dashboard/[id]/page.tsx`

Full rewrite of the page to add:
- Tab switcher (Tâches / Membres)
- Members tab with list, add-by-email form (OWNER only), role dropdown and remove button (OWNER only, can't remove self)
- Role-gated task controls (VIEWER sees tasks read-only; MEMBER/OWNER can create and edit)

- [ ] **Step 1: Rewrite `frontend/src/app/dashboard/[id]/page.tsx`**

```tsx
'use client'

import { useState } from 'react'
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { useParams, useRouter } from 'next/navigation'
import Link from 'next/link'
import { api } from '@/lib/api'
import type { Project, Task, TaskStatus, ProjectMember, Role } from '@/types/project'

const STATUS_LABELS: Record<TaskStatus, string> = {
  TODO: 'À faire',
  IN_PROGRESS: 'En cours',
  DONE: 'Terminé',
  BLOCKED: 'Bloqué',
  ARCHIVED: 'Archivé',
}

const STATUS_COLORS: Record<TaskStatus, string> = {
  TODO: 'bg-gray-500/20 text-gray-400',
  IN_PROGRESS: 'bg-blue-500/20 text-blue-400',
  DONE: 'bg-green-500/20 text-green-400',
  BLOCKED: 'bg-red-500/20 text-red-400',
  ARCHIVED: 'bg-gray-500/20 text-gray-500',
}

const ROLE_LABELS: Record<Role, string> = {
  OWNER: 'Propriétaire',
  MEMBER: 'Membre',
  VIEWER: 'Lecteur',
}

const ROLE_COLORS: Record<Role, string> = {
  OWNER: 'bg-purple-500/20 text-purple-400',
  MEMBER: 'bg-blue-500/20 text-blue-400',
  VIEWER: 'bg-gray-500/20 text-gray-400',
}

export default function ProjectDetailPage() {
  const { id } = useParams<{ id: string }>()
  const router = useRouter()
  const queryClient = useQueryClient()
  const [activeTab, setActiveTab] = useState<'tasks' | 'members'>('tasks')
  const [newTaskTitle, setNewTaskTitle] = useState('')
  const [showNewTask, setShowNewTask] = useState(false)
  const [newMemberEmail, setNewMemberEmail] = useState('')
  const [newMemberRole, setNewMemberRole] = useState<'MEMBER' | 'VIEWER'>('MEMBER')
  const [memberError, setMemberError] = useState<string | null>(null)

  const { data: project, isLoading: projectLoading } = useQuery<Project>({
    queryKey: ['project', id],
    queryFn: () => api.get(`/projects/${id}`).then(res => res.data.data),
  })

  const { data: tasks = [], isLoading: tasksLoading } = useQuery<Task[]>({
    queryKey: ['tasks', id],
    queryFn: () => api.get(`/projects/${id}/tasks`).then(res => res.data.data),
    enabled: activeTab === 'tasks',
  })

  const { data: members = [], isLoading: membersLoading } = useQuery<ProjectMember[]>({
    queryKey: ['members', id],
    queryFn: () => api.get(`/projects/${id}/members`).then(res => res.data.data),
    enabled: activeTab === 'members',
  })

  const createTask = useMutation({
    mutationFn: (title: string) =>
      api.post(`/projects/${id}/tasks`, { title }).then(res => res.data.data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['tasks', id] })
      setNewTaskTitle('')
      setShowNewTask(false)
    },
  })

  const updateTaskStatus = useMutation({
    mutationFn: ({ taskId, status }: { taskId: string; status: TaskStatus }) =>
      api.patch(`/projects/${id}/tasks/${taskId}`, { status }).then(res => res.data.data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['tasks', id] })
    },
  })

  const deleteProject = useMutation({
    mutationFn: () => api.delete(`/projects/${id}`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['projects'] })
      router.push('/dashboard')
    },
  })

  const addMember = useMutation({
    mutationFn: ({ email, role }: { email: string; role: 'MEMBER' | 'VIEWER' }) =>
      api.post(`/projects/${id}/members`, { email, role }).then(res => res.data.data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['members', id] })
      setNewMemberEmail('')
      setMemberError(null)
    },
    onError: (err: any) => {
      const msg = err?.response?.data?.error?.message
      setMemberError(
        msg === 'User not found' ? 'Utilisateur introuvable' :
        msg === 'Already a member' ? 'Déjà membre du projet' :
        "Erreur lors de l'ajout"
      )
    },
  })

  const updateMemberRole = useMutation({
    mutationFn: ({ userId, role }: { userId: string; role: Role }) =>
      api.patch(`/projects/${id}/members/${userId}`, { role }).then(res => res.data.data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['members', id] })
    },
  })

  const removeMember = useMutation({
    mutationFn: (userId: string) => api.delete(`/projects/${id}/members/${userId}`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['members', id] })
    },
  })

  if (projectLoading) {
    return (
      <main className="p-6 max-w-4xl mx-auto">
        <div className="animate-pulse">
          <div className="h-8 bg-gray-800 rounded w-48 mb-2" />
          <div className="h-4 bg-gray-800 rounded w-72 mb-8" />
          {[1, 2, 3].map(i => (
            <div key={i} className="h-16 bg-gray-800 rounded-xl mb-3" />
          ))}
        </div>
      </main>
    )
  }

  if (!project) {
    return (
      <main className="p-6 flex flex-col items-center justify-center gap-4 mt-20">
        <p className="text-gray-400">Projet introuvable</p>
        <Link href="/dashboard" className="text-purple-400 hover:text-purple-300 text-sm">
          ← Retour au dashboard
        </Link>
      </main>
    )
  }

  const userRole = project.userRole
  const isOwner = userRole === 'OWNER'
  const canEditTasks = userRole === 'OWNER' || userRole === 'MEMBER'
  const activeTasks = tasks.filter(t => t.status !== 'ARCHIVED')

  return (
    <main className="p-6 max-w-4xl mx-auto">
      {/* En-tête projet */}
      <div className="flex items-start justify-between mb-6">
        <div>
          <Link href="/dashboard" className="text-gray-500 hover:text-gray-400 text-sm mb-2 block">
            ← Dashboard
          </Link>
          <h1 className="text-2xl font-bold text-white">{project.name}</h1>
          {project.description && (
            <p className="text-gray-400 mt-1">{project.description}</p>
          )}
        </div>
        <div className="flex items-center gap-3">
          <span className={`text-xs px-2 py-1 rounded ${
            project.status === 'ACTIVE'
              ? 'bg-green-500/20 text-green-400'
              : 'bg-gray-500/20 text-gray-400'
          }`}>
            {project.status}
          </span>
          {isOwner && (
            <button
              onClick={() => {
                if (confirm('Supprimer ce projet ?')) deleteProject.mutate()
              }}
              className="text-red-400 hover:text-red-300 text-sm cursor-pointer"
            >
              Supprimer
            </button>
          )}
        </div>
      </div>

      {/* Onglets */}
      <div className="flex gap-1 mb-6 border-b border-gray-800">
        <button
          onClick={() => setActiveTab('tasks')}
          className={`px-4 py-2 text-sm font-medium cursor-pointer transition-colors ${
            activeTab === 'tasks'
              ? 'text-white border-b-2 border-purple-500'
              : 'text-gray-500 hover:text-gray-300'
          }`}
        >
          Tâches
        </button>
        <button
          onClick={() => setActiveTab('members')}
          className={`px-4 py-2 text-sm font-medium cursor-pointer transition-colors ${
            activeTab === 'members'
              ? 'text-white border-b-2 border-purple-500'
              : 'text-gray-500 hover:text-gray-300'
          }`}
        >
          Membres
        </button>
      </div>

      {/* Onglet Tâches */}
      {activeTab === 'tasks' && (
        <>
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-lg font-semibold text-white">
              Tâches <span className="text-gray-500 font-normal text-sm">({activeTasks.length})</span>
            </h2>
            {canEditTasks && (
              <button
                onClick={() => setShowNewTask(true)}
                className="bg-purple-500 hover:bg-purple-600 text-white px-3 py-1.5 rounded-lg text-sm cursor-pointer"
              >
                + Ajouter
              </button>
            )}
          </div>

          {showNewTask && (
            <div className="bg-gray-900 border border-purple-500/50 rounded-xl p-4 mb-3 flex gap-3">
              <input
                autoFocus
                value={newTaskTitle}
                onChange={e => setNewTaskTitle(e.target.value)}
                onKeyDown={e => {
                  if (e.key === 'Enter' && newTaskTitle.trim()) createTask.mutate(newTaskTitle.trim())
                  if (e.key === 'Escape') { setShowNewTask(false); setNewTaskTitle('') }
                }}
                placeholder="Titre de la tâche..."
                className="flex-1 bg-transparent text-white placeholder-gray-500 outline-none"
              />
              <button
                onClick={() => { if (newTaskTitle.trim()) createTask.mutate(newTaskTitle.trim()) }}
                disabled={!newTaskTitle.trim() || createTask.isPending}
                className="text-purple-400 hover:text-purple-300 text-sm disabled:opacity-50 cursor-pointer"
              >
                {createTask.isPending ? '...' : 'Ajouter'}
              </button>
              <button
                onClick={() => { setShowNewTask(false); setNewTaskTitle('') }}
                className="text-gray-500 hover:text-gray-400 text-sm cursor-pointer"
              >
                ✕
              </button>
            </div>
          )}

          {tasksLoading ? (
            <div className="flex flex-col gap-2">
              {[1, 2, 3].map(i => <div key={i} className="h-14 bg-gray-800 rounded-xl animate-pulse" />)}
            </div>
          ) : activeTasks.length === 0 ? (
            <div className="text-center py-16 text-gray-500">
              <p>Aucune tâche pour l'instant</p>
              {canEditTasks && (
                <button
                  onClick={() => setShowNewTask(true)}
                  className="text-purple-400 hover:text-purple-300 text-sm mt-2 cursor-pointer"
                >
                  Créer la première tâche →
                </button>
              )}
            </div>
          ) : (
            <div className="flex flex-col gap-2">
              {activeTasks.map(task => (
                <div
                  key={task.id}
                  className="bg-gray-900 border border-gray-800 rounded-xl px-4 py-3 flex items-center justify-between hover:border-gray-700"
                >
                  <span className={`text-white ${task.status === 'DONE' ? 'line-through text-gray-500' : ''}`}>
                    {task.title}
                  </span>
                  <select
                    value={task.status}
                    onChange={e => canEditTasks && updateTaskStatus.mutate({ taskId: task.id, status: e.target.value as TaskStatus })}
                    disabled={!canEditTasks}
                    className={`text-xs px-2 py-1 rounded border-0 outline-none ${STATUS_COLORS[task.status]} bg-transparent ${canEditTasks ? 'cursor-pointer' : 'cursor-default opacity-70'}`}
                  >
                    {(Object.keys(STATUS_LABELS) as TaskStatus[])
                      .filter(s => s !== 'ARCHIVED')
                      .map(s => (
                        <option key={s} value={s} className="bg-gray-900 text-white">
                          {STATUS_LABELS[s]}
                        </option>
                      ))}
                  </select>
                </div>
              ))}
            </div>
          )}
        </>
      )}

      {/* Onglet Membres */}
      {activeTab === 'members' && (
        <>
          {isOwner && (
            <div className="bg-gray-900 border border-gray-800 rounded-xl p-4 mb-6">
              <h3 className="text-sm font-medium text-gray-400 mb-3">Ajouter un membre</h3>
              <div className="flex gap-2">
                <input
                  value={newMemberEmail}
                  onChange={e => { setNewMemberEmail(e.target.value); setMemberError(null) }}
                  placeholder="email@exemple.com"
                  type="email"
                  className="flex-1 bg-gray-800 border border-gray-700 rounded-lg px-3 py-2 text-white placeholder-gray-500 text-sm focus:outline-none focus:border-purple-500"
                />
                <select
                  value={newMemberRole}
                  onChange={e => setNewMemberRole(e.target.value as 'MEMBER' | 'VIEWER')}
                  className="bg-gray-800 border border-gray-700 rounded-lg px-3 py-2 text-white text-sm focus:outline-none focus:border-purple-500 cursor-pointer"
                >
                  <option value="MEMBER">Membre</option>
                  <option value="VIEWER">Lecteur</option>
                </select>
                <button
                  onClick={() => {
                    if (newMemberEmail.trim()) {
                      addMember.mutate({ email: newMemberEmail.trim(), role: newMemberRole })
                    }
                  }}
                  disabled={!newMemberEmail.trim() || addMember.isPending}
                  className="bg-purple-500 hover:bg-purple-600 disabled:opacity-50 text-white px-4 py-2 rounded-lg text-sm cursor-pointer"
                >
                  {addMember.isPending ? '...' : 'Ajouter'}
                </button>
              </div>
              {memberError && <p className="text-red-400 text-sm mt-2">{memberError}</p>}
            </div>
          )}

          {membersLoading ? (
            <div className="flex flex-col gap-2">
              {[1, 2].map(i => <div key={i} className="h-14 bg-gray-800 rounded-xl animate-pulse" />)}
            </div>
          ) : (
            <div className="flex flex-col gap-2">
              {members.map(member => (
                <div
                  key={member.id}
                  className="bg-gray-900 border border-gray-800 rounded-xl px-4 py-3 flex items-center justify-between"
                >
                  <div>
                    <p className="text-white text-sm font-medium">{member.user.name || member.user.email}</p>
                    <p className="text-gray-500 text-xs">{member.user.email}</p>
                  </div>
                  <div className="flex items-center gap-2">
                    {isOwner && member.role !== 'OWNER' ? (
                      <>
                        <select
                          value={member.role}
                          onChange={e => updateMemberRole.mutate({ userId: member.userId, role: e.target.value as Role })}
                          className="bg-gray-800 border border-gray-700 rounded px-2 py-1 text-white text-xs focus:outline-none focus:border-purple-500 cursor-pointer"
                        >
                          <option value="MEMBER">Membre</option>
                          <option value="VIEWER">Lecteur</option>
                        </select>
                        <button
                          onClick={() => removeMember.mutate(member.userId)}
                          className="text-red-400 hover:text-red-300 text-xs cursor-pointer"
                        >
                          Retirer
                        </button>
                      </>
                    ) : (
                      <span className={`text-xs px-2 py-1 rounded ${ROLE_COLORS[member.role]}`}>
                        {ROLE_LABELS[member.role]}
                      </span>
                    )}
                  </div>
                </div>
              ))}
            </div>
          )}
        </>
      )}
    </main>
  )
}
```

- [ ] **Step 2: Verify in browser**

1. Open a project at http://localhost:3001/dashboard/[id] — should see "Tâches" and "Membres" tabs
2. Click "Membres" — should see yourself listed as "Propriétaire"
3. Add another user by email — they should appear in the list with a role badge
4. Change their role via the dropdown — should update immediately
5. Click "Retirer" — they should disappear from the list
6. Log in as a MEMBER user: "Ajouter" task button visible, task status editable, no add-member form
7. Log in as a VIEWER user: no "Ajouter" task button, task status selects disabled

- [ ] **Step 3: Commit**

```bash
git add frontend/src/app/dashboard/[id]/page.tsx
git commit -m "feat: add members tab with role-gated UI to project detail"
```

---

## Self-review

**Spec coverage:**
- Schema (Task 1) ✅
- `requireProjectRole` helper (Task 2) ✅
- POST /projects creates OWNER (Task 3) ✅
- GET /projects filters by membership + userRole (Task 4) ✅
- GET/DELETE /projects/:id role checks (Task 5) ✅
- Task routes updated (Task 6) ✅
- Member routes GET/POST/PATCH/DELETE (Tasks 7-8) ✅
- Frontend types (Task 9) ✅
- Dashboard role badge (Task 10) ✅
- Members tab + role-gated UI (Task 11) ✅

**Placeholder scan:** No TBDs, no vague steps. All steps have exact code or exact commands. ✅

**Type consistency:**
- `Role` defined in Task 9 as `"OWNER" | "MEMBER" | "VIEWER"`, used identically in Tasks 10 and 11 ✅
- `ProjectMember` defined in Task 9, imported in Task 11 ✅
- `requireProjectRole` returns `{ ok: false, response } | { ok: true, member }`, checked consistently in Tasks 5, 6, 7 ✅
- `ROLE_RANK` in Task 2 uses `Role` type matching the Prisma enum values ✅
