# V6 Real-time Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter des notifications live (SSE) pour tous les membres connectés à un projet quand une tâche ou un membre est créé, modifié ou supprimé.

**Architecture:** Un `EventBus` en mémoire (Map projectId → Set de senders) sur le backend. La route `GET /projects/:id/events` ouvre un stream SSE via Hono `streamSSE`, s'abonne au bus pour ce projet, et envoie un heartbeat toutes les 30s. Les routes d'écriture (tasks POST/PATCH/DELETE, members POST/DELETE) émettent un événement sur le bus après chaque opération réussie. Le frontend ouvre un `EventSource` via un hook custom, invalide TanStack Query et affiche un toast `react-hot-toast` sur chaque événement reçu. Les événements de l'utilisateur courant sont filtrés.

**Tech Stack:** Hono + ioredis + Prisma (backend port 3000), Next.js App Router + TanStack Query v5 + react-hot-toast (frontend port 3001). Package manager : **pnpm** (jamais npm).

---

## Files

**Backend — créés :**
- `src/lib/eventBus.ts` — pub/sub en mémoire : `subscribe`, `emit`
- `src/routes/events.ts` — `GET /` avec stream SSE + heartbeat

**Backend — modifiés :**
- `src/index.ts` — enregistrer la route events
- `src/routes/tasks.ts` — émettre `task.created/updated/deleted` après chaque écriture
- `src/routes/members.ts` — émettre `member.added/removed` après chaque écriture

**Frontend — créés :**
- `frontend/src/hooks/useProjectEvents.ts` — hook EventSource + invalidation + toast

**Frontend — modifiés :**
- `frontend/src/components/provider.tsx` — ajouter `<Toaster />`
- `frontend/src/app/dashboard/[id]/page.tsx` — appeler `useProjectEvents(id, currentUserName)`

---

## Task 1 : Créer le module EventBus

**Files:**
- Create: `src/lib/eventBus.ts`

- [ ] **Step 1 : Créer `src/lib/eventBus.ts`**

```ts
type EventType =
  | 'task.created'
  | 'task.updated'
  | 'task.deleted'
  | 'member.added'
  | 'member.removed'

export type ProjectEvent = {
  type: EventType
  actorName: string
  projectId: string
}

type SendFn = (payload: ProjectEvent) => void

const subscribers = new Map<string, Set<SendFn>>()

export const eventBus = {
  subscribe(projectId: string, fn: SendFn): () => void {
    if (!subscribers.has(projectId)) subscribers.set(projectId, new Set())
    subscribers.get(projectId)!.add(fn)
    return () => {
      subscribers.get(projectId)?.delete(fn)
      if (subscribers.get(projectId)?.size === 0) subscribers.delete(projectId)
    }
  },

  emit(projectId: string, payload: ProjectEvent): void {
    subscribers.get(projectId)?.forEach(fn => fn(payload))
  },
}
```

- [ ] **Step 2 : Vérifier que le projet compile**

```bash
pnpm tsc --noEmit
```

Expected output : aucune erreur TypeScript.

- [ ] **Step 3 : Commit**

```bash
git add src/lib/eventBus.ts
git commit -m "feat: add in-memory event bus for SSE pub/sub"
```

---

## Task 2 : Créer la route SSE et l'enregistrer

**Files:**
- Create: `src/routes/events.ts`
- Modify: `src/index.ts`

- [ ] **Step 1 : Créer `src/routes/events.ts`**

```ts
import { Hono } from 'hono'
import { streamSSE } from 'hono/streaming'
import { requireProjectRole } from '../lib/projectAuth'
import { eventBus } from '../lib/eventBus'

type Variables = { user: { id: string; email: string; name?: string | null } }

const events = new Hono<{ Variables: Variables }>()

events.get('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response

  return streamSSE(c, async (stream) => {
    const unsubscribe = eventBus.subscribe(id, (payload) => {
      stream.writeSSE({ data: JSON.stringify(payload) }).catch(() => {})
    })

    stream.onAbort(unsubscribe)

    while (!stream.aborted) {
      await stream.writeSSE({ event: 'heartbeat', data: '' })
      await stream.sleep(30_000)
    }
  })
})

export default events
```

- [ ] **Step 2 : Enregistrer la route dans `src/index.ts`**

Ajouter l'import en haut, avec les autres imports de routes :

```ts
import events from './routes/events'
```

Ajouter la route juste après `members`, avant `projects` :

```ts
app.route('/projects/:id/tasks', tasks)
app.route('/projects/:id/members', members)
app.route('/projects/:id/events', events)   // ← nouveau
app.route('/projects', projects)
```

- [ ] **Step 3 : Vérifier que le projet compile**

```bash
pnpm tsc --noEmit
```

Expected output : aucune erreur.

- [ ] **Step 4 : Tester l'endpoint SSE manuellement**

Démarrer le backend :
```bash
pnpm dev
```

Dans un terminal séparé, tester avec curl (remplacer `<session_cookie>` par un cookie valide copié depuis le navigateur) :

```bash
curl -N -H "Accept: text/event-stream" \
  --cookie "better-auth.session_token=<session_cookie>" \
  http://localhost:3000/projects/<project_id>/events
```

Expected output : connexion ouverte, puis toutes les 30s :
```
event: heartbeat
data:
```

Si pas de session valide, attendez la Task 6 pour tester via le navigateur.

- [ ] **Step 5 : Commit**

```bash
git add src/routes/events.ts src/index.ts
git commit -m "feat: add SSE events route with heartbeat and project auth"
```

---

## Task 3 : Émettre les événements dans les routes d'écriture

**Files:**
- Modify: `src/routes/tasks.ts`
- Modify: `src/routes/members.ts`

- [ ] **Step 1 : Ajouter l'import eventBus dans `src/routes/tasks.ts`**

Ajouter après les imports existants :

```ts
import { eventBus } from '../lib/eventBus'
```

- [ ] **Step 2 : Émettre `task.created` dans `tasks.post("/")`**

Remplacer `tasks.post("/", ...)` par :

```ts
tasks.post("/", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const body = await c.req.json()
  const result = CreateTaskSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const task = await db.task.create({
    data: {
      title: result.data.title,
      description: result.data.description,
      priority: result.data.priority,
      dueDate: result.data.dueDate,
      projectId: id,
    },
  })
  await cache.delPattern(`tasks:${id}:*`)
  const user = c.get('user')
  eventBus.emit(id, { type: 'task.created', actorName: user.name ?? user.email, projectId: id })
  return c.json({ data: task }, 201)
})
```

- [ ] **Step 3 : Émettre `task.updated` dans `tasks.patch("/:taskId")`**

Remplacer `tasks.patch("/:taskId", ...)` par :

```ts
tasks.patch("/:taskId", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const taskId = c.req.param("taskId")
  const body = await c.req.json()
  const result = UpdateTaskSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const task = await db.task.update({ where: { id: taskId }, data: result.data })
  await cache.delPattern(`tasks:${id}:*`)
  const user = c.get('user')
  eventBus.emit(id, { type: 'task.updated', actorName: user.name ?? user.email, projectId: id })
  return c.json({ data: task })
})
```

- [ ] **Step 4 : Émettre `task.deleted` dans `tasks.delete("/:taskId")`**

Remplacer `tasks.delete("/:taskId", ...)` par :

```ts
tasks.delete("/:taskId", async (c) => {
  const id = c.req.param("id")
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response
  const taskId = c.req.param("taskId")
  await db.task.update({ where: { id: taskId }, data: { status: "ARCHIVED" } })
  await cache.delPattern(`tasks:${id}:*`)
  const user = c.get('user')
  eventBus.emit(id, { type: 'task.deleted', actorName: user.name ?? user.email, projectId: id })
  return c.body(null, 204)
})
```

- [ ] **Step 5 : Ajouter l'import eventBus dans `src/routes/members.ts`**

Ajouter après les imports existants :

```ts
import { eventBus } from '../lib/eventBus'
```

- [ ] **Step 6 : Émettre `member.added` dans `members.post("/")`**

Remplacer `members.post('/', ...)` par :

```ts
members.post('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  const body = await c.req.json()
  const result = AddMemberSchema.safeParse(body)
  if (!result.success) return c.json({ error: result.error.flatten() }, 422)
  const targetUser = await db.user.findUnique({ where: { email: result.data.email } })
  if (!targetUser) return c.json({ error: { code: 'NOT_FOUND', message: 'User not found' } }, 404)
  const existing = await db.projectMember.findUnique({
    where: { projectId_userId: { projectId: id, userId: targetUser.id } },
  })
  if (existing) return c.json({ error: { code: 'CONFLICT', message: 'Already a member' } }, 409)
  const member = await db.projectMember.create({
    data: { projectId: id, userId: targetUser.id, role: result.data.role },
    include: { user: { select: { id: true, name: true, email: true } } },
  })
  const user = c.get('user')
  eventBus.emit(id, { type: 'member.added', actorName: user.name ?? user.email, projectId: id })
  return c.json({ data: member }, 201)
})
```

- [ ] **Step 7 : Émettre `member.removed` dans `members.delete("/:userId")`**

Remplacer `members.delete('/:userId', ...)` par :

```ts
members.delete('/:userId', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response
  const userId = c.req.param('userId')
  const currentUser = c.get('user') as { id: string }
  if (userId === currentUser.id) {
    return c.json({ error: { code: 'FORBIDDEN', message: 'Cannot remove yourself' } }, 403)
  }
  await db.projectMember.delete({
    where: { projectId_userId: { projectId: id, userId } },
  })
  const user = c.get('user')
  eventBus.emit(id, { type: 'member.removed', actorName: user.name ?? user.email, projectId: id })
  return c.body(null, 204)
})
```

- [ ] **Step 8 : Vérifier que le projet compile**

```bash
pnpm tsc --noEmit
```

Expected output : aucune erreur.

- [ ] **Step 9 : Commit**

```bash
git add src/routes/tasks.ts src/routes/members.ts
git commit -m "feat: emit SSE events after task and member writes"
```

---

## Task 4 : Installer react-hot-toast et ajouter le Toaster

**Files:**
- Modify: `frontend/src/components/provider.tsx`

- [ ] **Step 1 : Installer react-hot-toast dans le frontend**

```bash
cd frontend && pnpm add react-hot-toast
```

Expected output : `dependencies: + react-hot-toast x.x.x`

- [ ] **Step 2 : Ajouter `<Toaster />` dans `frontend/src/components/provider.tsx`**

Remplacer le contenu entier de `frontend/src/components/provider.tsx` par :

```tsx
'use client'

import { QueryClientProvider } from '@tanstack/react-query'
import { queryClient } from '@/lib/query-client'
import { Toaster } from 'react-hot-toast'

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <Toaster position="bottom-right" />
    </QueryClientProvider>
  )
}
```

- [ ] **Step 3 : Commit**

```bash
git add frontend/src/components/provider.tsx frontend/package.json frontend/pnpm-lock.yaml
git commit -m "feat: install react-hot-toast and add Toaster to providers"
```

---

## Task 5 : Créer le hook useProjectEvents

**Files:**
- Create: `frontend/src/hooks/useProjectEvents.ts`

- [ ] **Step 1 : Créer `frontend/src/hooks/useProjectEvents.ts`**

```ts
'use client'

import { useEffect } from 'react'
import { useQueryClient } from '@tanstack/react-query'
import toast from 'react-hot-toast'

type EventType =
  | 'task.created'
  | 'task.updated'
  | 'task.deleted'
  | 'member.added'
  | 'member.removed'

type ProjectEvent = {
  type: EventType
  actorName: string
  projectId: string
}

const MESSAGES: Record<EventType, (name: string) => string> = {
  'task.created': (n) => `${n} a créé une tâche`,
  'task.updated': (n) => `${n} a mis à jour une tâche`,
  'task.deleted': (n) => `${n} a supprimé une tâche`,
  'member.added': (n) => `${n} a ajouté un membre`,
  'member.removed': (n) => `${n} a retiré un membre`,
}

export function useProjectEvents(projectId: string, currentUserName?: string | null) {
  const queryClient = useQueryClient()

  useEffect(() => {
    const es = new EventSource(
      `http://localhost:3000/projects/${projectId}/events`,
      { withCredentials: true }
    )

    es.onmessage = (e) => {
      const event = JSON.parse(e.data) as ProjectEvent
      if (event.actorName === currentUserName) return

      if (event.type.startsWith('task.')) {
        queryClient.invalidateQueries({ queryKey: ['tasks', projectId] })
      } else {
        queryClient.invalidateQueries({ queryKey: ['members', projectId] })
      }

      const msg = MESSAGES[event.type]?.(event.actorName)
      if (msg) toast(msg)
    }

    return () => es.close()
  }, [projectId, currentUserName, queryClient])
}
```

- [ ] **Step 2 : Commit**

```bash
git add frontend/src/hooks/useProjectEvents.ts
git commit -m "feat: add useProjectEvents hook for SSE live notifications"
```

---

## Task 6 : Brancher le hook dans la page projet

**Files:**
- Modify: `frontend/src/app/dashboard/[id]/page.tsx`

- [ ] **Step 1 : Ajouter les imports**

En haut de `frontend/src/app/dashboard/[id]/page.tsx`, ajouter après les imports existants :

```ts
import { useProjectEvents } from '@/hooks/useProjectEvents'
import { authClient } from '@/lib/auth-client'
```

- [ ] **Step 2 : Récupérer la session et brancher le hook**

Dans le composant `ProjectDetailPage`, juste après les déclarations `const { id }`, `const router`, `const queryClient` et les états locaux, ajouter :

```ts
const { data: session } = authClient.useSession()
const currentUserName = session?.user?.name ?? session?.user?.email ?? undefined
useProjectEvents(id, currentUserName)
```

- [ ] **Step 3 : Vérifier que le projet compile**

```bash
cd frontend && pnpm tsc --noEmit
```

Expected output : aucune erreur.

- [ ] **Step 4 : Tester en navigateur**

Avec backend + frontend démarrés (`pnpm dev` dans les deux dossiers) :

1. Ouvrir le projet dans deux onglets différents (ou deux navigateurs, connectés avec deux utilisateurs différents si possible)
2. Dans l'onglet 1, créer une tâche
3. Dans l'onglet 2, un toast doit apparaître en bas à droite : `"[nom] a créé une tâche"` et la liste des tâches doit se rafraîchir sans rechargement
4. Dans l'onglet 1, changer le statut d'une tâche → toast `"... a mis à jour une tâche"` dans l'onglet 2
5. Dans l'onglet 1, ajouter un membre → toast `"... a ajouté un membre"` dans l'onglet 2
6. Vérifier dans les devtools (onglet Réseau, filtre EventStream) que la connexion SSE est bien établie

- [ ] **Step 5 : Commit**

```bash
git add "frontend/src/app/dashboard/[id]/page.tsx"
git commit -m "feat: wire useProjectEvents hook into project detail page"
```

---

## Self-review

**Spec coverage :**
- EventBus en mémoire (Task 1) ✅
- Route SSE avec auth + heartbeat (Task 2) ✅
- Émission après task POST/PATCH/DELETE (Task 3) ✅
- Émission après member POST/DELETE (Task 3) ✅
- Pas d'émission sur PATCH member (changement rôle) ✅
- react-hot-toast + Toaster (Task 4) ✅
- Hook useProjectEvents (Task 5) ✅
- Filtrage des propres événements via currentUserName (Task 5) ✅
- Invalidation TanStack Query (Task 5) ✅
- Branchement dans la page projet (Task 6) ✅

**Placeholder scan :** Aucun TBD. Tout le code est explicite. ✅

**Type consistency :**
- `ProjectEvent` défini dans `eventBus.ts` et exporté, utilisé dans `events.ts` via import ✅
- `ProjectEvent` redéfini localement dans le hook frontend (pas d'import cross-boundary) ✅
- `eventBus.subscribe` retourne `() => void`, utilisé tel quel dans `stream.onAbort` ✅
- `MESSAGES` couvre les 5 types de `EventType` — aucun type manquant ✅
