# V7a Fichiers — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter l'upload et le téléchargement de fichiers attachés aux projets et aux tâches, avec stockage sur disque local (`uploads/`).

**Architecture:** Nouveau modèle Prisma `Attachment` (nullable projectId ou taskId). Module `storage.ts` pour la gestion disque. Deux sets de routes Hono (fichiers projet + fichiers tâche). Côté frontend : hook TanStack Query + composants `FileUpload` / `FileList` / `TaskFilesSection` + nouvel onglet "Fichiers" dans la page projet.

**Tech Stack:** Hono (`c.req.parseBody()` multipart), Prisma (cuid IDs), Node.js `fs` + `crypto.randomUUID()`, Next.js App Router, TanStack Query v5, Axios FormData, Tailwind CSS.

## Global Constraints

- **pnpm uniquement** — jamais npm ni yarn. Toute commande d'installation utilise `pnpm`.
- Backend port 3000, frontend port 3001.
- Format réponse API : `{ "data": {...} }` ou `{ "error": { "code", "message" } }`.
- Limite fichier : 10 Mo (10 × 1024 × 1024 = 10 485 760 octets) — enforced backend ET frontend.
- IDs générés par Prisma `@default(cuid())` pour les entrées BDD ; noms sur disque via `crypto.randomUUID()`.
- TypeScript : le projet a ~20 erreurs TS préexistantes ignorées par `tsx` — ne pas les corriger, suivre les patterns existants.
- Commits Conventional Commits (`feat:`, `fix:`) après chaque tâche.

---

## File Map

**Créés :**
- `prisma/schema.prisma` — modifié : Attachment model + back-relations
- `src/lib/storage.ts` — save/delete/getPath pour fichiers disque
- `uploads/.gitkeep` — répertoire uploads versionné vide
- `src/routes/files.ts` — routes fichiers d'un projet
- `src/routes/taskFiles.ts` — routes fichiers d'une tâche
- `frontend/src/types/project.ts` — modifié : interface Attachment
- `frontend/src/hooks/useFiles.ts` — 6 hooks TanStack Query
- `frontend/src/components/FileUpload.tsx` — input file + validation
- `frontend/src/components/FileList.tsx` — liste + boutons download/delete + FileUpload
- `frontend/src/components/TaskFilesSection.tsx` — section expand/collapse par tâche

**Modifiés :**
- `.gitignore` — ajout `uploads/`
- `src/index.ts` — enregistrement des deux nouvelles routes
- `frontend/src/app/dashboard/[id]/page.tsx` — onglet Fichiers

---

### Task 1: Schéma Prisma — modèle Attachment + migration

**Files:**
- Modify: `prisma/schema.prisma`

**Interfaces:**
- Produces: modèle `Attachment` avec colonnes `id`, `filename`, `originalName`, `mimeType`, `size`, `createdAt`, `projectId?`, `taskId?`, `uploadedById`. Back-relations `attachments Attachment[]` sur `User`, `Project`, `Task`.

- [ ] **Step 1: Modifier `prisma/schema.prisma`**

Ajouter le modèle `Attachment` à la fin du fichier (avant la dernière ligne) et les back-relations sur les modèles existants :

```prisma
// Dans model User — ajouter après la ligne `projectMembers  ProjectMember[]` :
attachments     Attachment[]

// Dans model Project — ajouter après la ligne `members     ProjectMember[]` :
attachments     Attachment[]

// Dans model Task — ajouter après la ligne `@@index([projectId, status])` :
attachments     Attachment[]

// Nouveau modèle à ajouter à la fin du fichier :
model Attachment {
  id           String   @id @default(cuid())
  filename     String
  originalName String
  mimeType     String
  size         Int
  createdAt    DateTime @default(now())

  projectId    String?
  project      Project? @relation(fields: [projectId], references: [id], onDelete: Cascade)

  taskId       String?
  task         Task?    @relation(fields: [taskId], references: [id], onDelete: Cascade)

  uploadedById String
  uploadedBy   User     @relation(fields: [uploadedById], references: [id])
}
```

- [ ] **Step 2: Appliquer la migration**

```bash
pnpm prisma db push
```

Résultat attendu : `Your database is now in sync with your Prisma schema.`

- [ ] **Step 3: Régénérer le client Prisma**

```bash
pnpm prisma generate
```

Résultat attendu : `Generated Prisma Client` sans erreur.

- [ ] **Step 4: Vérifier la compilation TypeScript du backend**

```bash
pnpm exec tsc --noEmit 2>&1 | grep "Attachment" || echo "No Attachment errors"
```

Résultat attendu : `No Attachment errors` (les ~20 erreurs préexistantes sont tolérées, mais aucune nouvelle erreur sur Attachment).

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma generated/
git commit -m "feat: add Attachment model to Prisma schema"
```

---

### Task 2: Module storage + répertoire uploads

**Files:**
- Create: `src/lib/storage.ts`
- Create: `uploads/.gitkeep`
- Modify: `.gitignore`

**Interfaces:**
- Produces:
  ```ts
  storage.save(buffer: Buffer, originalName: string): string   // retourne le filename sur disque
  storage.delete(filename: string): void                        // silencieux si fichier absent
  storage.getPath(filename: string): string                     // chemin absolu
  ```

- [ ] **Step 1: Créer `src/lib/storage.ts`**

```ts
import { writeFileSync, unlinkSync, mkdirSync, existsSync } from 'fs'
import { join, extname } from 'path'
import { randomUUID } from 'crypto'

const UPLOADS_DIR = join(process.cwd(), 'uploads')

export const storage = {
  save(buffer: Buffer, originalName: string): string {
    if (!existsSync(UPLOADS_DIR)) mkdirSync(UPLOADS_DIR, { recursive: true })
    const ext = extname(originalName)
    const filename = `${randomUUID()}${ext}`
    writeFileSync(join(UPLOADS_DIR, filename), buffer)
    return filename
  },

  delete(filename: string): void {
    const path = join(UPLOADS_DIR, filename)
    if (existsSync(path)) unlinkSync(path)
  },

  getPath(filename: string): string {
    return join(UPLOADS_DIR, filename)
  },
}
```

- [ ] **Step 2: Créer `uploads/.gitkeep`**

Créer un fichier vide `uploads/.gitkeep` pour que git track le répertoire.

- [ ] **Step 3: Ajouter `uploads/` au `.gitignore`**

Ajouter ces deux lignes à la fin du `.gitignore` :

```
uploads/*
!uploads/.gitkeep
```

- [ ] **Step 4: Vérifier que le module compile**

```bash
pnpm exec tsc --noEmit 2>&1 | grep "storage" || echo "storage OK"
```

Résultat attendu : `storage OK`.

- [ ] **Step 5: Commit**

```bash
git add src/lib/storage.ts uploads/.gitkeep .gitignore
git commit -m "feat: add local disk storage module and uploads directory"
```

---

### Task 3: Routes fichiers projet (`src/routes/files.ts`)

**Files:**
- Create: `src/routes/files.ts`
- Modify: `src/index.ts`

**Interfaces:**
- Consumes:
  - `storage.save(buffer, originalName): string` — de `../lib/storage`
  - `storage.delete(filename): void`
  - `storage.getPath(filename): string`
  - `requireProjectRole(c, projectId, minRole)` — de `../lib/projectAuth`
  - `db` — de `../db/client` (accès `db.attachment`)
- Produces: Routes montées sur `/projects/:id/files` :
  - `GET /` → `{ data: Attachment[] }` (VIEWER+)
  - `POST /` → `{ data: Attachment }` 201 (MEMBER+)
  - `GET /:fileId` → fichier binaire avec headers Content-Disposition (VIEWER+)
  - `DELETE /:fileId` → 204 (OWNER)

- [ ] **Step 1: Créer `src/routes/files.ts`**

```ts
import { Hono } from 'hono'
import { readFileSync } from 'fs'
import { db } from '../db/client'
import { requireProjectRole } from '../lib/projectAuth'
import { storage } from '../lib/storage'

const MAX_SIZE = 10 * 1024 * 1024

type Variables = { user: { id: string; email: string; name?: string | null } }

const files = new Hono<{ Variables: Variables }>()

files.get('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response

  const attachments = await db.attachment.findMany({
    where: { projectId: id },
    orderBy: { createdAt: 'desc' },
    include: { uploadedBy: { select: { id: true, name: true, email: true } } },
  })
  return c.json({ data: attachments })
})

files.post('/', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response

  const body = await c.req.parseBody()
  const file = body['file']

  if (!file || typeof file === 'string') {
    return c.json({ error: { code: 'MISSING_FILE', message: 'No file provided' } }, 422)
  }

  if (file.size > MAX_SIZE) {
    return c.json({ error: { code: 'FILE_TOO_LARGE', message: 'File exceeds 10 MB limit' } }, 413)
  }

  const buffer = Buffer.from(await file.arrayBuffer())
  const filename = storage.save(buffer, file.name)
  const user = c.get('user')

  const attachment = await db.attachment.create({
    data: {
      filename,
      originalName: file.name,
      mimeType: file.type || 'application/octet-stream',
      size: file.size,
      projectId: id,
      uploadedById: user.id,
    },
    include: { uploadedBy: { select: { id: true, name: true, email: true } } },
  })

  return c.json({ data: attachment }, 201)
})

files.get('/:fileId', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response

  const fileId = c.req.param('fileId')
  const attachment = await db.attachment.findUnique({ where: { id: fileId } })
  if (!attachment || attachment.projectId !== id) {
    return c.json({ error: { code: 'NOT_FOUND', message: 'File not found' } }, 404)
  }

  const buffer = readFileSync(storage.getPath(attachment.filename))
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': attachment.mimeType,
      'Content-Disposition': `attachment; filename="${attachment.originalName}"`,
    },
  })
})

files.delete('/:fileId', async (c) => {
  const id = c.req.param('id')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response

  const fileId = c.req.param('fileId')
  const attachment = await db.attachment.findUnique({ where: { id: fileId } })
  if (!attachment || attachment.projectId !== id) {
    return c.json({ error: { code: 'NOT_FOUND', message: 'File not found' } }, 404)
  }

  storage.delete(attachment.filename)
  await db.attachment.delete({ where: { id: fileId } })
  return new Response(null, { status: 204 })
})

export default files
```

- [ ] **Step 2: Enregistrer la route dans `src/index.ts`**

Ajouter ces deux imports et lignes d'enregistrement **avant** `app.route('/projects', projects)` dans `src/index.ts`.

Localiser la ligne `import events from './routes/events'` et ajouter juste après :

```ts
import files from './routes/files'
```

Localiser la ligne `app.route('/projects/:id/events', events)` et ajouter juste après :

```ts
app.route('/projects/:id/files', files)
```

- [ ] **Step 3: Tester manuellement**

Démarrer le backend : `pnpm dev` (depuis la racine).

```bash
# Login (récupérer le cookie de session)
curl -c /tmp/cookies.txt -X POST http://localhost:3000/api/auth/sign-in/email \
  -H "Content-Type: application/json" \
  -d '{"email":"alice@test.com","password":"password123"}'

# Lister les fichiers d'un projet (adapter PROJECT_ID)
curl -b /tmp/cookies.txt http://localhost:3000/projects/PROJECT_ID/files
# Attendu : { "data": [] }

# Upload un fichier
curl -b /tmp/cookies.txt -X POST http://localhost:3000/projects/PROJECT_ID/files \
  -F "file=@/tmp/test.txt"
# Attendu : { "data": { "id": "...", "originalName": "test.txt", ... } }
```

- [ ] **Step 4: Commit**

```bash
git add src/routes/files.ts src/index.ts
git commit -m "feat: add project file upload/download routes"
```

---

### Task 4: Routes fichiers tâche (`src/routes/taskFiles.ts`)

**Files:**
- Create: `src/routes/taskFiles.ts`
- Modify: `src/index.ts`

**Interfaces:**
- Consumes: identique à Task 3 (`storage`, `requireProjectRole`, `db`)
- Produces: Routes montées sur `/projects/:id/tasks/:taskId/files` :
  - `GET /` → `{ data: Attachment[] }` (VIEWER+)
  - `POST /` → `{ data: Attachment }` 201 (MEMBER+)
  - `GET /:fileId` → fichier binaire (VIEWER+)
  - `DELETE /:fileId` → 204 (OWNER)
  - `c.req.param('id')` = projectId, `c.req.param('taskId')` = taskId (Hono propage les params du parent)

- [ ] **Step 1: Créer `src/routes/taskFiles.ts`**

```ts
import { Hono } from 'hono'
import { readFileSync } from 'fs'
import { db } from '../db/client'
import { requireProjectRole } from '../lib/projectAuth'
import { storage } from '../lib/storage'

const MAX_SIZE = 10 * 1024 * 1024

type Variables = { user: { id: string; email: string; name?: string | null } }

const taskFiles = new Hono<{ Variables: Variables }>()

taskFiles.get('/', async (c) => {
  const id = c.req.param('id')
  const taskId = c.req.param('taskId')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response

  const attachments = await db.attachment.findMany({
    where: { taskId },
    orderBy: { createdAt: 'desc' },
    include: { uploadedBy: { select: { id: true, name: true, email: true } } },
  })
  return c.json({ data: attachments })
})

taskFiles.post('/', async (c) => {
  const id = c.req.param('id')
  const taskId = c.req.param('taskId')
  const auth = await requireProjectRole(c, id, 'MEMBER')
  if (!auth.ok) return auth.response

  const body = await c.req.parseBody()
  const file = body['file']

  if (!file || typeof file === 'string') {
    return c.json({ error: { code: 'MISSING_FILE', message: 'No file provided' } }, 422)
  }

  if (file.size > MAX_SIZE) {
    return c.json({ error: { code: 'FILE_TOO_LARGE', message: 'File exceeds 10 MB limit' } }, 413)
  }

  const buffer = Buffer.from(await file.arrayBuffer())
  const filename = storage.save(buffer, file.name)
  const user = c.get('user')

  const attachment = await db.attachment.create({
    data: {
      filename,
      originalName: file.name,
      mimeType: file.type || 'application/octet-stream',
      size: file.size,
      taskId,
      uploadedById: user.id,
    },
    include: { uploadedBy: { select: { id: true, name: true, email: true } } },
  })

  return c.json({ data: attachment }, 201)
})

taskFiles.get('/:fileId', async (c) => {
  const id = c.req.param('id')
  const taskId = c.req.param('taskId')
  const auth = await requireProjectRole(c, id, 'VIEWER')
  if (!auth.ok) return auth.response

  const fileId = c.req.param('fileId')
  const attachment = await db.attachment.findUnique({ where: { id: fileId } })
  if (!attachment || attachment.taskId !== taskId) {
    return c.json({ error: { code: 'NOT_FOUND', message: 'File not found' } }, 404)
  }

  const buffer = readFileSync(storage.getPath(attachment.filename))
  return new Response(buffer, {
    status: 200,
    headers: {
      'Content-Type': attachment.mimeType,
      'Content-Disposition': `attachment; filename="${attachment.originalName}"`,
    },
  })
})

taskFiles.delete('/:fileId', async (c) => {
  const id = c.req.param('id')
  const taskId = c.req.param('taskId')
  const auth = await requireProjectRole(c, id, 'OWNER')
  if (!auth.ok) return auth.response

  const fileId = c.req.param('fileId')
  const attachment = await db.attachment.findUnique({ where: { id: fileId } })
  if (!attachment || attachment.taskId !== taskId) {
    return c.json({ error: { code: 'NOT_FOUND', message: 'File not found' } }, 404)
  }

  storage.delete(attachment.filename)
  await db.attachment.delete({ where: { id: fileId } })
  return new Response(null, { status: 204 })
})

export default taskFiles
```

- [ ] **Step 2: Enregistrer la route dans `src/index.ts`**

Ajouter juste après `import files from './routes/files'` :

```ts
import taskFiles from './routes/taskFiles'
```

Ajouter juste après `app.route('/projects/:id/files', files)` :

```ts
app.route('/projects/:id/tasks/:taskId/files', taskFiles)
```

- [ ] **Step 3: Tester manuellement**

Avec le serveur qui tourne, adapter PROJECT_ID et TASK_ID :

```bash
# Lister les fichiers d'une tâche
curl -b /tmp/cookies.txt http://localhost:3000/projects/PROJECT_ID/tasks/TASK_ID/files
# Attendu : { "data": [] }

# Upload
curl -b /tmp/cookies.txt -X POST \
  http://localhost:3000/projects/PROJECT_ID/tasks/TASK_ID/files \
  -F "file=@/tmp/test.txt"
# Attendu : { "data": { "id": "...", "taskId": "TASK_ID", ... } }
```

- [ ] **Step 4: Commit**

```bash
git add src/routes/taskFiles.ts src/index.ts
git commit -m "feat: add task file upload/download routes"
```

---

### Task 5: Types et hooks frontend

**Files:**
- Modify: `frontend/src/types/project.ts`
- Create: `frontend/src/hooks/useFiles.ts`

**Interfaces:**
- Consumes: `api` depuis `@/lib/api` (axios instance baseURL=http://localhost:3000, withCredentials:true)
- Produces:
  ```ts
  // Types
  interface Attachment { id, filename, originalName, mimeType, size, createdAt, projectId, taskId, uploadedById, uploadedBy }
  
  // Hooks projet
  useProjectFiles(projectId: string) → UseQueryResult<Attachment[]>
  useUploadProjectFile(projectId: string) → UseMutationResult<Attachment, unknown, File>
  useDeleteProjectFile(projectId: string) → UseMutationResult<unknown, unknown, string>
  
  // Hooks tâche
  useTaskFiles(projectId: string, taskId: string, opts?: { enabled?: boolean }) → UseQueryResult<Attachment[]>
  useUploadTaskFile(projectId: string, taskId: string) → UseMutationResult<Attachment, unknown, File>
  useDeleteTaskFile(projectId: string, taskId: string) → UseMutationResult<unknown, unknown, string>
  ```

- [ ] **Step 1: Ajouter l'interface `Attachment` dans `frontend/src/types/project.ts`**

Ajouter à la fin du fichier :

```ts
export interface Attachment {
  id: string
  filename: string
  originalName: string
  mimeType: string
  size: number
  createdAt: string
  projectId: string | null
  taskId: string | null
  uploadedById: string
  uploadedBy: {
    id: string
    name: string | null
    email: string
  }
}
```

- [ ] **Step 2: Créer `frontend/src/hooks/useFiles.ts`**

```ts
'use client'

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'
import { api } from '@/lib/api'
import type { Attachment } from '@/types/project'

export function useProjectFiles(projectId: string) {
  return useQuery<Attachment[]>({
    queryKey: ['files', 'project', projectId],
    queryFn: () => api.get(`/projects/${projectId}/files`).then(r => r.data.data),
  })
}

export function useUploadProjectFile(projectId: string) {
  const qc = useQueryClient()
  return useMutation<Attachment, unknown, File>({
    mutationFn: (file: File) => {
      const fd = new FormData()
      fd.append('file', file)
      return api.post(`/projects/${projectId}/files`, fd, {
        headers: { 'Content-Type': 'multipart/form-data' },
      }).then(r => r.data.data)
    },
    onSuccess: () => qc.invalidateQueries({ queryKey: ['files', 'project', projectId] }),
  })
}

export function useDeleteProjectFile(projectId: string) {
  const qc = useQueryClient()
  return useMutation<unknown, unknown, string>({
    mutationFn: (fileId: string) => api.delete(`/projects/${projectId}/files/${fileId}`),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['files', 'project', projectId] }),
  })
}

export function useTaskFiles(projectId: string, taskId: string, opts?: { enabled?: boolean }) {
  return useQuery<Attachment[]>({
    queryKey: ['files', 'task', taskId],
    queryFn: () => api.get(`/projects/${projectId}/tasks/${taskId}/files`).then(r => r.data.data),
    enabled: opts?.enabled ?? true,
  })
}

export function useUploadTaskFile(projectId: string, taskId: string) {
  const qc = useQueryClient()
  return useMutation<Attachment, unknown, File>({
    mutationFn: (file: File) => {
      const fd = new FormData()
      fd.append('file', file)
      return api.post(`/projects/${projectId}/tasks/${taskId}/files`, fd, {
        headers: { 'Content-Type': 'multipart/form-data' },
      }).then(r => r.data.data)
    },
    onSuccess: () => qc.invalidateQueries({ queryKey: ['files', 'task', taskId] }),
  })
}

export function useDeleteTaskFile(projectId: string, taskId: string) {
  const qc = useQueryClient()
  return useMutation<unknown, unknown, string>({
    mutationFn: (fileId: string) => api.delete(`/projects/${projectId}/tasks/${taskId}/files/${fileId}`),
    onSuccess: () => qc.invalidateQueries({ queryKey: ['files', 'task', taskId] }),
  })
}
```

- [ ] **Step 3: Vérifier la compilation TypeScript du frontend**

```bash
cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -E "useFiles|Attachment" || echo "Types OK"
```

Résultat attendu : `Types OK`.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/types/project.ts frontend/src/hooks/useFiles.ts
git commit -m "feat: add Attachment type and useFiles hooks"
```

---

### Task 6: Composants `FileUpload`, `FileList`, `TaskFilesSection`

**Files:**
- Create: `frontend/src/components/FileUpload.tsx`
- Create: `frontend/src/components/FileList.tsx`
- Create: `frontend/src/components/TaskFilesSection.tsx`

**Interfaces:**
- Consumes:
  - `Attachment` de `@/types/project`
  - `useTaskFiles`, `useUploadTaskFile`, `useDeleteTaskFile` de `@/hooks/useFiles`
  - `Task` de `@/types/project`
  - `NEXT_PUBLIC_API_URL` (env var, fallback `http://localhost:3000`)
- Produces:
  ```ts
  // FileUpload
  <FileUpload onUpload={(file: File) => void} isUploading: boolean disabled?: boolean />

  // FileList
  <FileList
    files: Attachment[]
    canUpload: boolean
    canDelete: boolean
    isUploading: boolean
    onUpload: (file: File) => void
    onDelete: (fileId: string) => void
    downloadBase: string   // ex: "http://localhost:3000/projects/:id/files"
  />

  // TaskFilesSection
  <TaskFilesSection
    projectId: string
    task: Task
    canUpload: boolean
    canDelete: boolean
  />
  ```

- [ ] **Step 1: Créer `frontend/src/components/FileUpload.tsx`**

```tsx
'use client'

import { useRef, useState } from 'react'

const MAX_SIZE = 10 * 1024 * 1024

interface Props {
  onUpload: (file: File) => void
  isUploading: boolean
  disabled?: boolean
}

export function FileUpload({ onUpload, isUploading, disabled }: Props) {
  const inputRef = useRef<HTMLInputElement>(null)
  const [error, setError] = useState<string | null>(null)

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0]
    if (!file) return
    if (file.size > MAX_SIZE) {
      setError('Fichier trop lourd (max 10 Mo)')
      e.target.value = ''
      return
    }
    setError(null)
    onUpload(file)
    e.target.value = ''
  }

  return (
    <div className="flex flex-col gap-1">
      <input
        ref={inputRef}
        type="file"
        onChange={handleChange}
        className="hidden"
        disabled={isUploading || disabled}
      />
      <button
        onClick={() => inputRef.current?.click()}
        disabled={isUploading || disabled}
        className="bg-purple-500 hover:bg-purple-600 disabled:opacity-50 text-white px-3 py-1.5 rounded-lg text-sm cursor-pointer w-fit"
      >
        {isUploading ? 'Envoi...' : '📎 Joindre un fichier'}
      </button>
      {error && <p className="text-red-400 text-xs">{error}</p>}
    </div>
  )
}
```

- [ ] **Step 2: Créer `frontend/src/components/FileList.tsx`**

```tsx
'use client'

import type { Attachment } from '@/types/project'
import { FileUpload } from './FileUpload'

function formatSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} o`
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} Ko`
  return `${(bytes / (1024 * 1024)).toFixed(1)} Mo`
}

interface Props {
  files: Attachment[]
  canUpload: boolean
  canDelete: boolean
  isUploading: boolean
  onUpload: (file: File) => void
  onDelete: (fileId: string) => void
  downloadBase: string
}

export function FileList({ files, canUpload, canDelete, isUploading, onUpload, onDelete, downloadBase }: Props) {
  return (
    <div className="flex flex-col gap-2">
      {files.length === 0 && (
        <p className="text-gray-500 text-sm py-2">Aucun fichier joint</p>
      )}
      {files.map(file => (
        <div
          key={file.id}
          className="bg-gray-900 border border-gray-800 rounded-xl px-4 py-3 flex items-center justify-between"
        >
          <div>
            <p className="text-white text-sm font-medium">{file.originalName}</p>
            <p className="text-gray-500 text-xs">
              {formatSize(file.size)} · {new Date(file.createdAt).toLocaleDateString('fr-FR')}
              {file.uploadedBy && ` · ${file.uploadedBy.name ?? file.uploadedBy.email}`}
            </p>
          </div>
          <div className="flex items-center gap-3">
            <a
              href={`${downloadBase}/${file.id}`}
              className="text-purple-400 hover:text-purple-300 text-xs"
            >
              Télécharger
            </a>
            {canDelete && (
              <button
                onClick={() => onDelete(file.id)}
                className="text-red-400 hover:text-red-300 text-xs cursor-pointer"
              >
                Supprimer
              </button>
            )}
          </div>
        </div>
      ))}
      {canUpload && (
        <div className="mt-1">
          <FileUpload onUpload={onUpload} isUploading={isUploading} />
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 3: Créer `frontend/src/components/TaskFilesSection.tsx`**

Ce composant gère ses propres hooks (évite les hooks dans une boucle dans le parent).

```tsx
'use client'

import { useState } from 'react'
import type { Task } from '@/types/project'
import { useTaskFiles, useUploadTaskFile, useDeleteTaskFile } from '@/hooks/useFiles'
import { FileList } from './FileList'

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3000'

interface Props {
  projectId: string
  task: Task
  canUpload: boolean
  canDelete: boolean
}

export function TaskFilesSection({ projectId, task, canUpload, canDelete }: Props) {
  const [expanded, setExpanded] = useState(false)
  const { data: files = [] } = useTaskFiles(projectId, task.id, { enabled: expanded })
  const upload = useUploadTaskFile(projectId, task.id)
  const del = useDeleteTaskFile(projectId, task.id)

  return (
    <div className="bg-gray-900 border border-gray-800 rounded-xl overflow-hidden">
      <button
        onClick={() => setExpanded(e => !e)}
        className="w-full flex items-center justify-between px-4 py-3 text-left cursor-pointer hover:bg-gray-800/50"
      >
        <span className="text-white text-sm font-medium">{task.title}</span>
        <span className="text-gray-500 text-xs">{expanded ? '▲' : '▼'}</span>
      </button>
      {expanded && (
        <div className="px-4 pb-4">
          <FileList
            files={files}
            canUpload={canUpload}
            canDelete={canDelete}
            isUploading={upload.isPending}
            onUpload={file => upload.mutate(file)}
            onDelete={fileId => del.mutate(fileId)}
            downloadBase={`${API_URL}/projects/${projectId}/tasks/${task.id}/files`}
          />
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: Vérifier la compilation TypeScript du frontend**

```bash
cd frontend && pnpm exec tsc --noEmit 2>&1 | grep -E "FileUpload|FileList|TaskFiles" || echo "Components OK"
```

Résultat attendu : `Components OK`.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/FileUpload.tsx frontend/src/components/FileList.tsx frontend/src/components/TaskFilesSection.tsx
git commit -m "feat: add FileUpload, FileList, and TaskFilesSection components"
```

---

### Task 7: Intégration dans `dashboard/[id]/page.tsx`

**Files:**
- Modify: `frontend/src/app/dashboard/[id]/page.tsx`

**Interfaces:**
- Consumes:
  - `useProjectFiles`, `useUploadProjectFile`, `useDeleteProjectFile` de `@/hooks/useFiles`
  - `FileList` de `@/components/FileList`
  - `TaskFilesSection` de `@/components/TaskFilesSection`
  - `NEXT_PUBLIC_API_URL` (env var, fallback `http://localhost:3000`)
  - `activeTab` étendu : `'tasks' | 'members' | 'files'`
  - `tasks` query : `enabled` étendu à `activeTab === 'tasks' || activeTab === 'files'`
  - `project.userRole` : `'OWNER' | 'MEMBER' | 'VIEWER'` pour canUpload / canDelete

**Résultat attendu :** Troisième onglet "Fichiers" visible dans la page projet. Section "Fichiers du projet" + section "Fichiers par tâche" avec expand/collapse par tâche.

- [ ] **Step 1: Ajouter les imports dans `frontend/src/app/dashboard/[id]/page.tsx`**

Ajouter ces imports après les imports existants (après la ligne `import { authClient } from '@/lib/auth-client'`) :

```ts
import { useProjectFiles, useUploadProjectFile, useDeleteProjectFile } from '@/hooks/useFiles'
import { FileList } from '@/components/FileList'
import { TaskFilesSection } from '@/components/TaskFilesSection'

const API_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3000'
```

- [ ] **Step 2: Étendre le type de `activeTab` et les queries**

Remplacer :

```ts
const [activeTab, setActiveTab] = useState<'tasks' | 'members'>('tasks')
```

Par :

```ts
const [activeTab, setActiveTab] = useState<'tasks' | 'members' | 'files'>('tasks')
```

Remplacer dans la query tasks :

```ts
enabled: activeTab === 'tasks',
```

Par :

```ts
enabled: activeTab === 'tasks' || activeTab === 'files',
```

- [ ] **Step 3: Ajouter les queries et mutations fichiers projet**

Ajouter après la déclaration de `removeMember` (avant le bloc `if (projectLoading)`) :

```ts
const { data: projectFiles = [] } = useProjectFiles(id)
const uploadProjectFile = useUploadProjectFile(id)
const deleteProjectFile = useDeleteProjectFile(id)
```

- [ ] **Step 4: Ajouter la variable `canUpload` et `canDelete`**

Après les lignes `const isOwner = ...` et `const canEditTasks = ...` (ligne ~158 dans la version actuelle), ajouter :

```ts
const canUpload = userRole === 'OWNER' || userRole === 'MEMBER'
const canDelete = userRole === 'OWNER'
```

- [ ] **Step 5: Ajouter le bouton onglet "Fichiers"**

Dans le bloc `{/* Onglets */}`, après le bouton "Membres" (trouver la balise fermante `</button>` du bouton Membres), ajouter :

```tsx
<button
  onClick={() => setActiveTab('files')}
  className={`px-4 py-2 text-sm font-medium cursor-pointer transition-colors ${
    activeTab === 'files'
      ? 'text-white border-b-2 border-purple-500'
      : 'text-gray-500 hover:text-gray-300'
  }`}
>
  Fichiers
</button>
```

- [ ] **Step 6: Ajouter le contenu de l'onglet Fichiers**

Trouver la fin du JSX qui rend le contenu des tabs (après le bloc `{activeTab === 'members' && (...)}`) et ajouter :

```tsx
{activeTab === 'files' && (
  <div className="flex flex-col gap-6">
    {/* Fichiers du projet */}
    <div>
      <h3 className="text-lg font-semibold text-white mb-3">Fichiers du projet</h3>
      <FileList
        files={projectFiles}
        canUpload={canUpload}
        canDelete={canDelete}
        isUploading={uploadProjectFile.isPending}
        onUpload={file => uploadProjectFile.mutate(file)}
        onDelete={fileId => deleteProjectFile.mutate(fileId)}
        downloadBase={`${API_URL}/projects/${id}/files`}
      />
    </div>

    {/* Fichiers par tâche */}
    <div>
      <h3 className="text-lg font-semibold text-white mb-3">Fichiers par tâche</h3>
      {tasks.length === 0 ? (
        <p className="text-gray-500 text-sm">Aucune tâche dans ce projet</p>
      ) : (
        <div className="flex flex-col gap-2">
          {tasks.map(task => (
            <TaskFilesSection
              key={task.id}
              projectId={id}
              task={task}
              canUpload={canUpload}
              canDelete={canDelete}
            />
          ))}
        </div>
      )}
    </div>
  </div>
)}
```

- [ ] **Step 7: Vérifier la compilation TypeScript**

```bash
cd frontend && pnpm exec tsc --noEmit 2>&1 | grep "dashboard" || echo "Page OK"
```

Résultat attendu : `Page OK` (ou erreurs préexistantes unrelated au fichier page.tsx).

- [ ] **Step 8: Tester manuellement dans le navigateur**

1. Démarrer backend : `pnpm dev` (racine)
2. Démarrer frontend : `cd frontend && pnpm dev`
3. Ouvrir http://localhost:3001 → Login avec alice@test.com / password123
4. Ouvrir un projet → vérifier que l'onglet "Fichiers" apparaît
5. Cliquer "Fichiers" → vérifier les deux sections
6. Uploader un fichier dans "Fichiers du projet" → vérifier qu'il apparaît dans la liste
7. Cliquer "Télécharger" → vérifier que le fichier se télécharge
8. Cliquer "Supprimer" → vérifier que le fichier disparaît
9. Déplier une tâche → uploader un fichier sur la tâche → vérifier

- [ ] **Step 9: Commit**

```bash
git add frontend/src/app/dashboard/[id]/page.tsx
git commit -m "feat: add Fichiers tab with project and task file management"
```

---

## Self-Review

### 1. Spec coverage

| Exigence spec | Tâche |
|---|---|
| Modèle Attachment (projectId?, taskId?, uploadedById) | Task 1 |
| Back-relations User/Project/Task | Task 1 |
| onDelete Cascade | Task 1 |
| `src/lib/storage.ts` save/delete/getPath | Task 2 |
| `uploads/` créé auto + `.gitignore` | Task 2 |
| `GET /projects/:id/files` (VIEWER+) | Task 3 |
| `POST /projects/:id/files` (MEMBER+) | Task 3 |
| `GET /projects/:id/files/:fileId` download (VIEWER+) | Task 3 |
| `DELETE /projects/:id/files/:fileId` (OWNER) | Task 3 |
| Routes tâche identiques | Task 4 |
| Vérification taille 10 Mo + 413 | Task 3, 4 |
| Vérification fichier présent + 422 | Task 3, 4 |
| Content-Disposition: attachment | Task 3, 4 |
| Type Attachment frontend | Task 5 |
| Hooks useProjectFiles, useUploadProjectFile, useDeleteProjectFile | Task 5 |
| Hooks useTaskFiles, useUploadTaskFile, useDeleteTaskFile | Task 5 |
| Composant FileUpload (validation 10 Mo + erreur inline) | Task 6 |
| Composant FileList (liste + download + delete + upload zone) | Task 6 |
| Onglet Fichiers dans page projet | Task 7 |
| Section fichiers projet | Task 7 |
| Section fichiers par tâche (expand/collapse) | Task 7 |
| VIEWER=download, MEMBER+=upload, OWNER=delete | Tasks 3, 4, 7 |

### 2. Vérification des placeholders

Aucun TBD, TODO, ou "implement later" dans le plan.

### 3. Cohérence des types

- `storage.save()` → `string` utilisé comme `filename` dans les routes → cohérent.
- `Attachment.uploadedBy` inclus dans les queries `include` backend → correspond à l'interface frontend.
- `useTaskFiles(projectId, taskId, { enabled })` → `TaskFilesSection` passe `{ enabled: expanded }` → cohérent.
- `downloadBase` dans `FileList` + `TaskFilesSection` utilise `API_URL` → cohérent.
